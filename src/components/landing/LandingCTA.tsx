
import React from 'react';
import { Button } from '@/components/ui/button';
import { CheckCircle, ArrowRight, Zap, Clock, Shield, Sparkles, TrendingUp } from 'lucide-react';

interface LandingCTAProps {
  onStartDemo: () => void;
  user: any;
}

const LandingCTA = ({ onStartDemo, user }: LandingCTAProps) => {
  const guarantees = [
    { icon: Zap, text: "Business registered in 5-7 days or money back" },
    { icon: Shield, text: "100% compliance guarantee - we handle all violations" },
    { icon: Clock, text: "24/7 expert support included in all plans" }
  ];

  return (
    <section className="relative py-20 bg-gradient-to-br from-slate-950 via-violet-950 to-slate-900 overflow-hidden">
      {/* Simplified Background */}
      <div className="absolute inset-0">
        <div className="absolute top-20 left-20 w-96 h-96 bg-violet-500/10 rounded-full blur-3xl"></div>
        <div className="absolute bottom-20 right-20 w-96 h-96 bg-cyan-500/10 rounded-full blur-3xl"></div>
      </div>

      <div className="relative z-10 max-w-6xl mx-auto text-center px-4 sm:px-6 lg:px-8">
        {/* Simplified Urgency Banner */}
        <div className="inline-flex items-center bg-violet-500/20 text-violet-300 px-8 py-4 rounded-full text-sm font-semibold mb-12 border border-violet-500/30">
          <Sparkles className="mr-2" size={16} />
          🔥 Limited Time: 50% Off Setup Fees This Month
        </div>

        <h2 className="text-4xl lg:text-5xl xl:text-6xl font-bold text-white mb-8 leading-tight">
          Ready to Join Nigeria's 
          <span className="block bg-gradient-to-r from-violet-400 to-cyan-400 bg-clip-text text-transparent">
            Fastest-Growing Businesses?
          </span>
        </h2>
        
        <p className="text-lg lg:text-xl xl:text-2xl text-slate-300 mb-12 max-w-4xl mx-auto leading-relaxed">
          Join thousands of entrepreneurs who've transformed their ideas into thriving, compliant businesses with BizCore. 
          <span className="text-white font-semibold"> Start your journey today and be operational by week's end.</span>
        </p>

        {/* Single Stats Section */}
        <div className="bg-white/5 backdrop-blur-md rounded-3xl p-8 mb-12 border border-white/20">
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
            <div className="text-white">
              <div className="text-3xl mb-3">✨</div>
              <div className="text-sm font-medium text-slate-300">1,847 businesses launched this year</div>
            </div>
            <div className="text-white">
              <div className="text-3xl mb-3">💰</div>
              <div className="text-sm font-medium text-slate-300">₦200M+ in revenue generated by our users</div>
            </div>
            <div className="text-white">
              <div className="text-3xl mb-3">📈</div>
              <div className="text-sm font-medium text-slate-300">Average 67% revenue growth in first 6 months</div>
            </div>
          </div>
        </div>

        {/* Main CTA */}
        <div className="mb-12">
          <Button 
            onClick={onStartDemo}
            className="bg-gradient-to-r from-violet-600 to-cyan-600 hover:from-violet-500 hover:to-cyan-500 text-white px-16 py-8 rounded-3xl text-2xl font-bold transition-all transform hover:scale-105 shadow-2xl hover:shadow-violet-500/25 duration-300 w-full sm:w-auto"
          >
            {user ? 'Enter Your Dashboard →' : 'Start Your Business Journey →'}
            <ArrowRight className="ml-4" size={24} />
          </Button>
          
          <div className="mt-8 space-y-4">
            <p className="text-violet-300 text-lg font-medium">
              100-day free trial • No credit card required • Setup starts immediately
            </p>
            <p className="text-cyan-200 text-sm flex items-center justify-center gap-2">
              <TrendingUp size={16} />
              Join today and get your business registered by Friday ⚡
            </p>
          </div>
        </div>

        {/* Simplified Guarantees */}
        <div className="bg-white/5 backdrop-blur-md rounded-3xl p-10 border border-white/20">
          <h3 className="text-3xl font-bold text-white mb-8">Our Ironclad Guarantees</h3>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {guarantees.map((guarantee, index) => (
              <div key={index} className="flex flex-col items-center text-center space-y-4 group">
                <div className="p-4 bg-white/10 rounded-2xl group-hover:bg-white/20 transition-all duration-300">
                  <guarantee.icon className="text-violet-400 group-hover:text-violet-300 transition-colors" size={28} />
                </div>
                <p className="text-white font-medium leading-relaxed group-hover:text-violet-300 transition-colors">{guarantee.text}</p>
              </div>
            ))}
          </div>
        </div>

        {/* Simplified Social Proof */}
        <div className="mt-16 pt-8 border-t border-white/20">
          <div className="flex flex-col md:flex-row items-center justify-center gap-8 text-slate-300">
            <div className="flex items-center gap-3">
              <div className="flex">
                {[...Array(5)].map((_, i) => (
                  <span key={i} className="text-yellow-400 text-2xl">⭐</span>
                ))}
              </div>
              <span className="font-medium">4.9/5 from 3,200+ reviews</span>
            </div>
            <div className="font-medium">SOC 2 Compliant • Bank-level security</div>
          </div>
        </div>
      </div>
    </section>
  );
};

export default LandingCTA;
